<% flash.each do |key, value| %>
  <% if key == 'danger' %>
    <div class="<%= flash_class(key) %>">
      <%= value.join("<br/>").html_safe %>
    </div>
  <% else %>
    <div class="<%= flash_class(key) %>">
      <%= value %>
    </div>
  <% end %>
<% end %>
